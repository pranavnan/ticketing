name: tests-orders
on:
  pull_request:
    paths: # This will only run if the files inside the orders project changes
      - 'orders/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd orders && npm install && npm run test:ci
