name: tests-auth
on:
  pull_request:
    paths: # This will only run if the files inside the auth project changes
      - 'auth/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd auth && npm install && npm run test:ci
